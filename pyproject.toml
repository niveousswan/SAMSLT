[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "samslt-monorepo"
version = "0.3.0"
description = "SAMSLT monorepo metadata"
requires-python = ">=3.11"
